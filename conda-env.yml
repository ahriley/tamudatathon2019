name: datathon

channels:
  - conda-forge
  - defaults

dependencies:
  # channel-specific
  - geopy
  - plotly
  - pip:
      - uszipcode

  # core scientific python
  - python
  - pip
  - numpy
  - scipy
  - pandas

  # plotting packages
  - matplotlib
  - seaborn

  # for my workflow
  - jupyter
  - jupyterlab
